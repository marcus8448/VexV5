#ifndef AUTONOMOUS_HPP
#define AUTONOMOUS_HPP

/**
 * Drops a preloaded ring into the right side alliance goal and pulls the goal.
 */
void right_side_winpoint();

/**
 * Drops a preloaded ring into the left side alliance goal and pulls the goal.
 */
void left_side_winpoint();

/**
 * Drives straight towards the middle-right goal and pulls it back.
 */
void middle_right_goal();

/**
 * Drives straight towards the middle-left goal and pulls it back.
 */
void middle_left_goal();

#endif